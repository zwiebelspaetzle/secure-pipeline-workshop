name: AI Security Analysis

on:
  workflow_call:

jobs:
  # 🚧 REPLACE THIS ENTIRE 'jobs:' SECTION WITH WORKSHOP CONTENT! 🚧
  # Copy from: workshop/ai_scan/{tool}/workflow.yml

  ai-scan:
    name: "🚧 AI Scan - Workshop Placeholder"
    runs-on: ubuntu-latest
    outputs:
      result: ${{ steps.placeholder.outputs.result }}
    steps:
      - name: Workshop Placeholder
        id: placeholder
        run: |
          echo "🚧 Replace this job with content from workshop/ai_scan/{tool}/workflow.yml!"
          echo "This will implement: AI-powered security analysis with the chosen tool"
          echo "result=workshop-placeholder" >> "$GITHUB_OUTPUT"
